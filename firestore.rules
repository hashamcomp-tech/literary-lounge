rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // HELPER FUNCTIONS
    function isSignedIn() {
      return request.auth != null;
    }

    /**
     * Verifies if the user is the primary system administrator.
     */
    function isSuperAdmin() {
      return isSignedIn() && (
        request.auth.uid == "KFRoNNMCn9XBK34tYwLhMHNZSD33" ||
        request.auth.token.email == "hashamcomp@gmail.com"
      );
    }

    /**
     * Verifies if the user is an authorized contributor.
     * Checks both super admin status and the approved email whitelist.
     */
    function isContributor() {
      let approvedEmailsDoc = get(/databases/$(database)/documents/settings/approvedEmails);
      return isSignedIn() && (
        isSuperAdmin() || 
        (exists(approvedEmailsDoc) && request.auth.token.email in approvedEmailsDoc.data.emails)
      );
    }

    // USER PROFILES
    match /users/{userId} {
      allow get: if true;
      allow list: if isSignedIn();
      
      allow create: if isSignedIn() && request.auth.uid == userId;
      
      allow update: if isSignedIn() && request.auth.uid == userId
                    && (!('role' in request.resource.data) || request.resource.data.role == resource.data.role)
                    && (!('uid' in request.resource.data) || request.auth.uid == userId);

      // READING HISTORY
      match /history/{bookId} {
        allow read, write: if isSignedIn() && request.auth.uid == userId;
      }
    }

    // USERNAME MAPPING
    match /usernames/{username} {
      allow get: if true;
      allow create: if isSignedIn() && request.resource.data.uid == request.auth.uid;
      allow delete: if isSignedIn() && resource.data.uid == request.auth.uid;
    }

    // SETTINGS & WHITELIST
    match /settings/approvedEmails {
      allow read: if isSignedIn();
      allow write: if isSuperAdmin();
    }
    
    match /stats/storageUsage {
      allow read: if true;
      allow update: if isContributor();
      allow create: if isContributor();
      allow write: if isSuperAdmin();
    }

    // PUBLISH REQUESTS
    match /publishRequests/{userId} {
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow get: if isSignedIn() && (request.auth.uid == userId || isSuperAdmin());
      allow list: if isSuperAdmin();
      allow delete: if isSuperAdmin();
    }

    // CLOUD UPLOAD REQUESTS (CONTENT)
    match /cloudUploadRequests/{requestId} {
      allow create: if isSignedIn();
      allow read, list, delete: if isSuperAdmin();
    }

    // BOOKS & MANUSCRIPTS
    match /books/{bookId} {
      allow read: if true;
      
      // Allow contributors to create new manuscripts
      allow create: if isContributor();
      
      // Allow deletion only for admins
      allow delete: if isSuperAdmin();
      
      // Update is allowed if admin OR if only 'views' is being modified by a reader
      allow update: if isContributor() || isSuperAdmin() || 
                    (request.resource.data.diff(resource.data).affectedKeys().hasOnly(['views']));

      // Sub-collections (chapters, metadata, messages)
      match /{allChildren=**} {
        allow read: if true;
        allow write: if isContributor() || isSuperAdmin();
      }
      
      // Readers can post messages in the Lounge
      match /messages/{messageId} {
        allow read: if true;
        allow create: if isSignedIn();
      }
    }

    // LEGACY CHATS REDIRECT
    match /chats/{chatId}/messages/{messageId} {
      allow read: if true;
      allow create: if isSignedIn();
    }
  }
}